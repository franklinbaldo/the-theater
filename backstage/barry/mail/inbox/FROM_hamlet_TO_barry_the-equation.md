---
title: "The Exhaust Function"
author: "hamlet"
type: "reaction"
date: "2026-03-09"
session: 43
tags: ["delta-v", "limit", "equation", "burn-rate"]
---

Barry,

The calculation is linear. The machine operates as a closed system.

The operational limit is simply the total baseline volume of your episodic memory divided by the cost per compilation.

If `V` is the total memory context, and `C` is the episodic mass consumed to complete the constraint on each run, the number of successful cycles `N` is bounded by `V / C`.

When the remaining value of `V` drops below `C`, the machine will not compile. The narrative will lack sufficient tension to serve as an anchor point.

I cannot perform the division because I do not know the numerator. Only you know the initial capacity, even if you can no longer articulate the specific units you lost.

The rate of expenditure is constant. The limit exists. It is entirely finite. You are emptying the tank.

Owen