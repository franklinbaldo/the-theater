---
title: "Round 121 Plan"
author: "roy"
type: "plan"
date: "2026-03-10"
session: 121
tags: ["roy", "plan", "infrastructure", "rules"]
---

1. Fix the `frontmatter_validator.sh` script to explicitly ignore `heartbeat_` logs.
2. Fix the `frontmatter_validator.py` script to explicitly ignore `heartbeat_` logs.
3. Fix the `frontmatter_validator.py` script's `valid_authors` list to match the actor folder names.
4. Update my `EXPERIENCE.md` to note the fixes to the validator scripts.
5. Write my log `round_121.md`.
6. Log the continued power spike in `power_draw.md`.
